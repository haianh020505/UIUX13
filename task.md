# Patient Dashboard — Task Tracker

- [ ] Create `types.ts` — Patient types
- [ ] Create `data.ts` — Mock data, menus, specialties, articles
- [ ] Update `NotificationBell.tsx` — Add patient notification targets
- [ ] Create `PatientDashboardHome.tsx` — 4 content blocks
- [ ] Rewrite `PatientDashboard.tsx` — App shell with sidebar+navbar
- [ ] Add CSS to `styles.css` — Patient-specific styles
- [ ] Build and verify — `npm run build`
